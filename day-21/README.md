# Day 21: Kaggle Capstone 获奖项目分析

## 获奖项目一览

| 名次 | 项目 | 链接 |
|------|------|------|
| 🥇 1st | NewsPulse AI Agent | [GitHub](https://github.com/azhang6-nlp/NewsPulse_AI_Agent) |
| 🥈 2nd | FIL Content Agent System | [Agent](https://github.com/jasononaquest/fil-agent) / [MCP](https://github.com/jasononaquest/FIL-mcp) |
| 🥉 3rd | AishIngAnalyzer | [GitHub](https://github.com/aishasartaj1/AishIngAnalyzer) |

---

## 🥇 NewsPulse AI Agent - 设计模式分析

> 自主、自纠错的多 Agent 新闻研究与简报系统

### 解决的问题

高管和 AI 从业者每天收到数百条通用提醒，信息噪音严重。需要**个性化、准确、及时**的情报简报。

### 核心架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    NewsPulse Multi-Agent System                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐  │
│  │ Profiler │───▶│ Retriever│───▶│ Verifier │───▶│Generator │  │
│  │  Agent   │    │  Agent   │    │  Agent   │    │  Agent   │  │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘  │
│       │                                                │         │
│       │         ┌──────────┐                          │         │
│       └────────▶│ Evaluator│◀─────────────────────────┘         │
│                 │  Agent   │                                     │
│                 └────┬─────┘                                     │
│                      │                                           │
│                      ▼                                           │
│                 ┌──────────┐                                     │
│                 │ Feedback │ ◀── 用户反馈学习                    │
│                 │  Loop    │                                     │
│                 └──────────┘                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 设计模式亮点

| 模式 | 说明 | 价值 |
|------|------|------|
| **Multi-Agent 编排** | 使用 Google ADK 的 LLM 驱动多 Agent 框架 | 职责分离，各司其职 |
| **Self-Correction Loop** | LLM 评估器循环检查质量 | 零幻觉，输出可信 |
| **Citation Verification** | 所有内容必须有引用来源 | 可追溯，可验证 |
| **User Profiling** | 根据用户偏好个性化内容 | 精准推送，减少噪音 |
| **Feedback Learning** | 从用户反馈中持续学习 | 越用越智能 |

### 关键技术点

```python
# 典型的 Self-Correction 模式
class QualityEvaluator:
    def evaluate(self, newsletter):
        """LLM 评估生成内容的质量"""
        score = self.llm.evaluate(
            content=newsletter,
            criteria=[
                "factual_accuracy",
                "citation_presence",
                "relevance_to_user",
                "writing_quality"
            ]
        )
        if score < THRESHOLD:
            return {"status": "regenerate", "feedback": score.details}
        return {"status": "approved"}
```

---

## 🥈 FIL Content Agent System - 设计模式分析

> Agent + MCP Server 分离架构

### 架构特点

```
┌─────────────────┐         ┌─────────────────┐
│   FIL Agent     │◀───────▶│   FIL MCP       │
│  (Agent 逻辑)    │   A2A   │  (工具服务器)    │
└─────────────────┘         └─────────────────┘
        │                           │
        ▼                           ▼
   决策与对话               数据获取与处理
```

### 设计模式亮点

| 模式 | 说明 |
|------|------|
| **关注点分离** | Agent 专注决策，MCP Server 专注工具能力 |
| **MCP 标准化** | 使用标准 MCP 协议，工具可复用 |
| **独立部署** | Agent 和 MCP Server 可独立扩展 |

---

## 🥉 AishIngAnalyzer - 设计模式分析

> 食品成分分析 Agent

### 典型的 Multimodal Agent 模式

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  Image   │────▶│   OCR    │────▶│ Analysis │
│  Input   │     │ Extract  │     │  Agent   │
└──────────┘     └──────────┘     └──────────┘
                                       │
                                       ▼
                                 ┌──────────┐
                                 │  Health  │
                                 │  Report  │
                                 └──────────┘
```

---

## 优秀 Agent 设计的共同特征

### 1. 明确的问题定义

```
❌ "做一个通用 AI 助手"
✅ "为高管提供零幻觉的个性化 AI 新闻简报"
```

### 2. Multi-Agent 协作模式

```python
# 推荐：职责单一的多 Agent
agents = {
    "retriever": RetrieveAgent(),    # 只负责获取
    "verifier": VerifyAgent(),       # 只负责验证
    "generator": GenerateAgent(),    # 只负责生成
    "evaluator": EvaluateAgent(),    # 只负责评估
}

# 避免：一个 Agent 做所有事
# giant_agent = DoEverythingAgent()  # ❌
```

### 3. 自我纠错机制

```python
MAX_RETRIES = 3

for attempt in range(MAX_RETRIES):
    result = agent.generate(task)
    evaluation = evaluator.check(result)

    if evaluation.passed:
        return result

    # 将评估反馈注入下一轮
    task.add_context(evaluation.feedback)

raise QualityError("无法达到质量标准")
```

### 4. 引用与可追溯性

```python
class VerifiedContent:
    content: str
    citations: list[Citation]  # 必须有来源
    confidence: float

    def validate(self):
        """每个声明都必须有对应引用"""
        for claim in self.extract_claims():
            assert claim.has_citation(), f"未引用: {claim}"
```

### 5. 用户反馈闭环

```
用户使用 ──▶ 收集反馈 ──▶ 分析模式 ──▶ 调整行为
    ▲                                      │
    └──────────────────────────────────────┘
```

---

## 设计检查清单

在构建 Agent 时，问自己：

- [ ] 问题定义是否足够具体？
- [ ] 是否需要拆分成多个专职 Agent？
- [ ] 有没有质量评估和自纠错机制？
- [ ] 输出是否可追溯（有引用来源）？
- [ ] 是否有用户反馈的学习机制？
- [ ] 工具能力是否通过 MCP 标准化？

---

## 参考资源

- [NewsPulse AI Agent](https://github.com/azhang6-nlp/NewsPulse_AI_Agent)
- [FIL Agent](https://github.com/jasononaquest/fil-agent) | [FIL MCP](https://github.com/jasononaquest/FIL-mcp)
- [AishIngAnalyzer](https://github.com/aishasartaj1/AishIngAnalyzer)
- [Google ADK Multi-Agent 文档](https://google.github.io/adk-docs/)
- [MCP 工具集成](https://google.github.io/adk-docs/tools-custom/mcp-tools/)
